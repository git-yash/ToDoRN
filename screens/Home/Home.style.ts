import {StyleSheet} from 'react-native';

const homeStyle = StyleSheet.create({
  welcomeText: {
    fontSize: 25,
    padding: 15,
    fontWeight: 'bold',
    color: '#465881',
  },
});

export default homeStyle;
